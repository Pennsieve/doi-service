name: Sync OAS to ReadMe
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: readmeio/github-readme-sync@v2
        with:
          readme-oas-key: KC9Vh7XrlXjYuIDoFlGHKEdXPo2BFK1V:609548d06de4e5fe58aabcdc
           
          # OPTIONAL CONFIG, use if necessary
          oas-file-path: './swagger/doi-service.yml'
          # api-version: 'v1.0.0'
